# Deployment And Local Development

This directory is reserved for local multi-node development tooling and later
deployment manifests.

The first local environment should run one client daemon, one to three
coordinators, five to twenty storage nodes, one bootstrap node, one relay node,
one gateway, and optional repair/indexer roles.
